<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.faculdade.buscacurso">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/buscaCurso">
        <activity android:name=".CursosFavoritos"></activity>
        <activity android:name=".CadastroCurso" />
        <activity android:name=".Home" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CadastroCorporativo" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CadastroUsuario" />
        <activity android:name=".HomeCorporativo" />
        <activity android:name=".TipoCurso" />
        <activity android:name=".VerCursos" />
        <activity android:name=".CursoInfo"></activity>
        <activity android:name=".PerfilUsuario"></activity>
        <activity android:name=".CursoInfo" />
    </application>

</manifest>